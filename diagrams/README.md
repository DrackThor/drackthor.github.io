# diagrams

Editable diagram **sources** (`.drawio`, etc.) for the knowledge base.

This folder lives outside `src/` and `public/`, so nothing here is ever built or shipped to the site.
Export rendered images (PNG/SVG) into `src/content/kb/_img/` and reference those from the articles.
